<%= form_for(@web_slide) do |f| %>
  <% if @web_slide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@web_slide.errors.count, "error") %> prohibited this web_slide from being saved:</h2>

      <ul>
      <% @web_slide.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :site_url %><br />
    <%= f.text_field :site_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
