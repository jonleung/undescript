<%= form_for(@rss_slide) do |f| %>
  <% if @rss_slide.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@rss_slide.errors.count, "error") %> prohibited this rss_slide from being saved:</h2>

      <ul>
      <% @rss_slide.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :rss_url %><br />
    <%= f.text_field :rss_url %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
